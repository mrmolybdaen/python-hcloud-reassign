[build-system]
requires = [
  "setuptools >= 75.1.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "hcloud_reassign"
dynamic = ["version"]
requires-python = ">= 3.11"
dependencies = [
    "hcloud>=2.3.0",
]
